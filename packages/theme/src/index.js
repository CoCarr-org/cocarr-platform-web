// Design tokens usable from JS. The Tailwind layer lives in theme.css, which
// each app imports from its own globals.css.
export const BRAND = {
  50: '#f5f7ff', 500: '#4f46e5', 600: '#4338ca', 700: '#3730a3',
};
export const FONT_SANS = 'Inter, ui-sans-serif, system-ui, sans-serif';
